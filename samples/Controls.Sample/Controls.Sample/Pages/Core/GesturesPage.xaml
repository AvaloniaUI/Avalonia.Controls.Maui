<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Maui.Controls.Sample.Pages.GesturesPage"
    xmlns:controls="using:Maui.Controls.Sample.Controls"
    xmlns:views="clr-namespace:Maui.Controls.Sample.Pages.Base"
    xmlns:viewmodels="clr-namespace:Maui.Controls.Sample.ViewModels">
    <views:BasePage.BindingContext>
        <viewmodels:GesturesViewModel />
    </views:BasePage.BindingContext>
    <!-- TODO: Create a ControlTemplate -->
    <Grid
        RowSpacing="0"
        RowDefinitions="48, 20, Auto, *">
        <!-- HEADER -->
        <Image
            Grid.Row="0"
            Aspect="AspectFill"
            Source="header_background"/>
        <!-- SEARCH -->
        <Frame
            Grid.Row="0"
            Grid.RowSpan="2"
            Style="{StaticResource SearchBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <controls:BordelessEntry
                    Grid.Column="1"
                    Placeholder="Filter"
                    Text="{Binding FilterValue, Mode=OneWayToSource}"
                    ReturnCommand="{Binding FilterCommand}"
                    ReturnType="Search"
                    PlaceholderColor="{AppThemeBinding Light={StaticResource LightTextSecondaryColor}, Dark={StaticResource DarkTextSecondaryColor}}"
                    TextColor="{AppThemeBinding Light={StaticResource LightTextPrimaryColor}, Dark={StaticResource DarkTextPrimaryColor}}"
                    FontSize="Medium"
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="Center"/>
            </Grid>
        </Frame>
        <!-- NOTICE -->
        <Border
            Grid.Row="2"
            Background="#33FFA500"
            Stroke="Orange"
            StrokeThickness="1"
            Padding="10"
            Margin="6, 12, 6, 0">
            <Label
                Text="⚠ Temporarily unavailable: gesture recognizers are pending a public MAUI IGesturePlatformManager API (dotnet/maui#33364), so these galleries will not respond. See docs/known-issues.md."
                FontSize="13"/>
        </Border>
        <!-- CONTENT -->
        <CollectionView
            x:Name="GesturesSections"
            Grid.Row="3"
            BackgroundColor="Transparent"
            ItemsSource="{Binding FilteredItems}"
            SelectionMode="Single"
            SelectedItem="{Binding SelectedItem, Source={x:RelativeSource AncestorType={x:Type ContentPage}}, Mode=TwoWay}"
            SelectionChangedCommand="{Binding NavigateCommand, Source={x:RelativeSource AncestorType={x:Type ContentPage}}}"
            Margin="6, 12, 6, 0">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout  
                    Orientation="Vertical"
                    ItemSpacing="6"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border     
                        Style="{StaticResource GalleryItemContainerStyle}">
                        <Border.StrokeShape>
                            <RoundRectangle 
                                CornerRadius="0" />
                        </Border.StrokeShape>
                        <Grid>
                            <StackLayout>
                                <Label
                                    Text="{Binding Title}"
                                    Style="{StaticResource GalleryItemTitleStyle}"/>
                                <Label
                                    Text="{Binding Description}"
                                    Style="{StaticResource GalleryItemDescriptionStyle}"/>
                            </StackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</views:BasePage>