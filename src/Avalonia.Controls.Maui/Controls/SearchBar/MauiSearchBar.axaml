<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="using:Avalonia.Controls.Maui.Controls"
                    x:ClassModifier="internal">

    <ResourceDictionary.ThemeDictionaries>
        <ResourceDictionary x:Key="Light">
            <SolidColorBrush x:Key="SearchBarBackground" Color="#FFFFFFFF" />
            <SolidColorBrush x:Key="SearchBarBorderBrush" Color="#FF8A8A8A" />
            <SolidColorBrush x:Key="SearchBarForeground" Color="#FF000000" />
            <SolidColorBrush x:Key="SearchBarPlaceholderForeground" Color="#FF8A8A8A" />
        </ResourceDictionary>
        <ResourceDictionary x:Key="Dark">
            <SolidColorBrush x:Key="SearchBarBackground" Color="#FF2B2B2B" />
            <SolidColorBrush x:Key="SearchBarBorderBrush" Color="#FF5A5A5A" />
            <SolidColorBrush x:Key="SearchBarForeground" Color="#FFFFFFFF" />
            <SolidColorBrush x:Key="SearchBarPlaceholderForeground" Color="#FFB0B0B0" />
        </ResourceDictionary>
    </ResourceDictionary.ThemeDictionaries>

    <ControlTheme x:Key="{x:Type controls:MauiSearchBar}"
                  TargetType="controls:MauiSearchBar">
        <Setter Property="Background" Value="{DynamicResource SearchBarBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource SearchBarBorderBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource SearchBarForeground}" />
        <Setter Property="PlaceholderForeground" Value="{DynamicResource SearchBarPlaceholderForeground}" />
        <Setter Property="SearchIconColor" Value="{DynamicResource SearchBarForeground}" />
        <Setter Property="CancelButtonColor" Value="{DynamicResource SearchBarForeground}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="4"
                    Padding="4">

                    <Grid ColumnDefinitions="Auto,*,Auto,Auto">

                        <Button
                            Name="PART_SearchIconButton"
                            Grid.Column="0"
                            Width="24"
                            Height="24"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="4"
                            VerticalAlignment="Center"
                            AutomationProperties.Name="{TemplateBinding QueryIconHelpText}">
                            <Panel>
                                <PathIcon
                                    Name="PART_SearchIcon"
                                    Width="16"
                                    Height="16"
                                    Foreground="{TemplateBinding SearchIconColor}"
                                    Data="M15.5 14h-.79l-.28-.27A6.471 6.471 0 0 0 16 9.5 6.5 6.5 0 1 0 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
                                    IsVisible="{Binding SearchIcon, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static ObjectConverters.IsNull}}" />
                                <ContentPresenter
                                    Content="{TemplateBinding SearchIcon}"
                                    IsVisible="{Binding SearchIcon, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static ObjectConverters.IsNotNull}}" />
                            </Panel>
                        </Button>

                        <TextBox
                            Name="PART_TextBox"
                            Grid.Column="1"
                            BorderThickness="0"
                            Background="Transparent"
                            Foreground="{TemplateBinding Foreground}"
                            FontSize="{TemplateBinding FontSize}"
                            FontFamily="{TemplateBinding FontFamily}"
                            FontWeight="{TemplateBinding FontWeight}"
                            FontStyle="{TemplateBinding FontStyle}"
                            LetterSpacing="{TemplateBinding LetterSpacing}"
                            VerticalAlignment="Stretch"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />

                        <TextBlock
                            Name="PART_Placeholder"
                            Grid.Column="1"
                            Text="{TemplateBinding Placeholder}"
                            Foreground="{TemplateBinding PlaceholderForeground}"
                            FontSize="{TemplateBinding FontSize}"
                            FontFamily="{TemplateBinding FontFamily}"
                            FontWeight="{TemplateBinding FontWeight}"
                            FontStyle="{TemplateBinding FontStyle}"
                            LetterSpacing="{TemplateBinding LetterSpacing}"
                            VerticalAlignment="Center"
                            TextAlignment="{TemplateBinding HorizontalTextAlignment}"
                            IsHitTestVisible="False"
                            IsVisible="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static StringConverters.IsNullOrEmpty}}"
                            Margin="6,0,6,0" />

                        <Button
                            Name="PART_ClearButton"
                            Grid.Column="2"
                            Width="20"
                            Height="20"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="4"
                            Margin="4,0"
                            IsVisible="False"
                            IsEnabled="{TemplateBinding IsClearEnabled}"
                            Command="{TemplateBinding ClearCommand}"
                            CommandParameter="{TemplateBinding ClearCommandParameter}"
                            VerticalAlignment="Center"
                            AutomationProperties.Name="{TemplateBinding ClearIconHelpText}">
                            <Panel>
                                <PathIcon
                                    Name="PART_ClearIcon"
                                    Width="10"
                                    Height="10"
                                    Foreground="{TemplateBinding CancelButtonColor}"
                                    Data="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"
                                    IsVisible="{Binding ClearIcon, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static ObjectConverters.IsNull}}" />
                                <ContentPresenter
                                    Content="{TemplateBinding ClearIcon}"
                                    IsVisible="{Binding ClearIcon, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static ObjectConverters.IsNotNull}}" />
                            </Panel>
                        </Button>

                        <Button
                            Name="PART_SearchButton"
                            Grid.Column="3"
                            Content="Search"
                            IsVisible="False"
                            Margin="4,0" />
                    </Grid>
                </Border>
            </ControlTemplate>
        </Setter>
    </ControlTheme>
</ResourceDictionary>
