<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AlohaAI.ViewModels"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             x:Class="AlohaAI.Views.LessonPage"
             x:DataType="vm:LessonViewModel"
             Title="{Binding Title}"
             Shell.NavBarIsVisible="False"
             Shell.NavBarVisibilityAnimationEnabled="False"
             Shell.TabBarIsVisible="False"
             BackgroundColor="{AppThemeBinding Dark={StaticResource PageBg_Dark}, Light={StaticResource PageBg_Light}}"
             SafeAreaEdges="None">
    <Grid Margin="0,0,0,0" SafeAreaEdges="None">
        <!-- Purple gradient background -->
        <BoxView InputTransparent="True">
            <BoxView.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="{AppThemeBinding Dark={StaticResource GradientTop_Dark}, Light={StaticResource GradientTop_Light}}" Offset="0.0" />
                    <GradientStop Color="{AppThemeBinding Dark={StaticResource PageBg_Dark}, Light={StaticResource PageBg_Light}}" Offset="1.0" />
                </LinearGradientBrush>
            </BoxView.Background>
        </BoxView>

        <!-- Header image fading out -->
        <Grid HeightRequest="260" VerticalOptions="Start" InputTransparent="True">
            <Image Source="{Binding HeaderImage}" Aspect="AspectFill" />
            <BoxView>
                <BoxView.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#00000000" Offset="0.0" />
                        <GradientStop Color="{AppThemeBinding Dark={StaticResource GradientOverlayMid_Dark}, Light={StaticResource GradientOverlayMid_Light}}" Offset="0.5" />
                        <GradientStop Color="{AppThemeBinding Dark={StaticResource GradientOverlayEnd_Dark}, Light={StaticResource GradientOverlayEnd_Light}}" Offset="1.0" />
                    </LinearGradientBrush>
                </BoxView.Background>
            </BoxView>
        </Grid>

        <!-- XP Earned Popup (above everything) -->
        <Border x:Name="XpPopup"
                IsVisible="False"
                Opacity="0"
                BackgroundColor="{StaticResource XPColor}"
                StrokeShape="RoundRectangle 20"
                Stroke="Transparent"
                Padding="20,12"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                ZIndex="10">
            <Border.Shadow>
                <Shadow Brush="{StaticResource XPColor}" Offset="0,4" Radius="16" Opacity="0.5" />
            </Border.Shadow>
            <Label x:Name="XpPopupLabel" Text="+25 XP" FontSize="28" FontAttributes="Bold" TextColor="White" HorizontalOptions="Center" />
        </Border>

        <!-- Everything scrolls -->
        <ScrollView VerticalScrollBarVisibility="Never">
            <VerticalStackLayout Spacing="0">

                <!-- Header -->
                <VerticalStackLayout Padding="20,70,20,16" Spacing="6">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <Border BackgroundColor="{AppThemeBinding Dark={StaticResource OverlayBg_Dark}, Light={StaticResource OverlayBg_Light}}" StrokeShape="RoundRectangle 12" Stroke="Transparent"
                                WidthRequest="36" HeightRequest="36"
                                HorizontalOptions="Start" VerticalOptions="Center">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding GoBackCommand}" />
                            </Border.GestureRecognizers>
                            <Path Data="M14,5 L6,12 L14,19" Stroke="{AppThemeBinding Dark={StaticResource TextPrimary_Dark}, Light={StaticResource TextPrimary_Light}}" StrokeThickness="2"
                                  StrokeLineCap="Round" StrokeLineJoin="Round"
                                  Aspect="Uniform" HeightRequest="14" WidthRequest="14"
                                  HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <Label Grid.Column="1" Text="{Binding ModuleTitle}" FontSize="22" FontAttributes="Bold"
                               TextColor="{AppThemeBinding Dark={StaticResource TextPrimary_Dark}, Light={StaticResource TextPrimary_Light}}" MaxLines="2" LineBreakMode="WordWrap" VerticalOptions="Center">
                            <Label.Shadow>
                                <Shadow Brush="#40000000" Offset="0,1" Radius="4" Opacity="0.4" />
                            </Label.Shadow>
                        </Label>
                    </Grid>
                    <HorizontalStackLayout Spacing="8" Padding="48,0,0,0">
                        <Label Text="{Binding PathTitle}" FontSize="13" TextColor="{AppThemeBinding Dark=#BBBBBB, Light=#6E6580}" />
                        <Label Text="·" FontSize="13" TextColor="{AppThemeBinding Dark={StaticResource TextTertiary_Dark}, Light={StaticResource TextTertiary_Light}}" />
                        <Label Text="{Binding LessonPositionText}" FontSize="13" TextColor="{AppThemeBinding Dark=#BBBBBB, Light=#6E6580}" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>

                <!-- Progress Bar -->
                <Grid HeightRequest="4" Margin="20,0,20,16">
                    <Border BackgroundColor="{AppThemeBinding Dark=#18FFFFFF, Light=#15000000}" StrokeShape="RoundRectangle 2" Stroke="Transparent" />
                    <Border StrokeShape="RoundRectangle 2" Stroke="Transparent"
                            HorizontalOptions="Start"
                            BackgroundColor="{StaticResource Primary}"
                            WidthRequest="{Binding LessonProgress, Converter={StaticResource ProgressToWidthConverter}, ConverterParameter=335}">
                        <Border.Shadow>
                            <Shadow Brush="{StaticResource Primary}" Offset="0,0" Radius="4" Opacity="0.5" />
                        </Border.Shadow>
                    </Border>
                </Grid>

                <!-- Content Section -->
                <Label Text="Content" FontSize="17" FontAttributes="Bold"
                       TextColor="{AppThemeBinding Dark={StaticResource TextPrimary_Dark}, Light={StaticResource TextPrimary_Light}}" Padding="20,0,20,10">
                    <Label.Shadow>
                        <Shadow Brush="#40000000" Offset="0,1" Radius="2" Opacity="0.3" />
                    </Label.Shadow>
                </Label>

                <!-- Article Content Card (dark glassmorphism) -->
                <Border Margin="16,0,16,16" StrokeShape="RoundRectangle 18" StrokeThickness="0.8" Padding="18,16">
                    <Border.BackgroundColor>
                        <AppThemeBinding Dark="{StaticResource CardBgAlt_Dark}" Light="{StaticResource CardBg_Light}" />
                    </Border.BackgroundColor>
                    <Border.Stroke>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{AppThemeBinding Dark=#40FFFFFF, Light=#30000000}" Offset="0.0" />
                            <GradientStop Color="{AppThemeBinding Dark={StaticResource GlassBorderEnd_Dark}, Light={StaticResource GlassBorderEnd_Light}}" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Stroke>
                    <Border.Shadow>
                        <Shadow Brush="{AppThemeBinding Dark={StaticResource ShadowColor_Dark}, Light={StaticResource ShadowColor_Light}}" Offset="0,8" Radius="24" Opacity="0.4" />
                    </Border.Shadow>
                    <VerticalStackLayout Spacing="0">
                        <!-- Markdown Content -->
                        <VerticalStackLayout x:Name="ContentArea" Spacing="10" />

                        <!-- Keywords Chips -->
                        <FlexLayout x:Name="KeywordsArea" Wrap="Wrap" Padding="0,12,0,0"
                                    JustifyContent="Start" AlignItems="Center" Direction="Row">
                        </FlexLayout>
                    </VerticalStackLayout>
                </Border>

                <!-- Action Card -->
                <Border Margin="16,0,16,16" StrokeShape="RoundRectangle 18" StrokeThickness="0.8" Padding="18,16">
                    <Border.BackgroundColor>
                        <AppThemeBinding Dark="{StaticResource CardBgAlt_Dark}" Light="{StaticResource CardBg_Light}" />
                    </Border.BackgroundColor>
                    <Border.Stroke>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{AppThemeBinding Dark=#30FFFFFF, Light=#20000000}" Offset="0.0" />
                            <GradientStop Color="{AppThemeBinding Dark={StaticResource GlassBorderEnd_Dark}, Light={StaticResource GlassBorderEnd_Light}}" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Stroke>
                    <Border.Shadow>
                        <Shadow Brush="{AppThemeBinding Dark={StaticResource ShadowColor_Dark}, Light={StaticResource ShadowColor_Light}}" Offset="0,6" Radius="20" Opacity="0.35" />
                    </Border.Shadow>
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                        <VerticalStackLayout VerticalOptions="Center" Spacing="4">
                            <Label Text="{Binding Title}" FontSize="15" FontAttributes="Bold" TextColor="{AppThemeBinding Dark={StaticResource TextPrimary_Dark}, Light={StaticResource TextPrimary_Light}}"
                                   MaxLines="1" LineBreakMode="TailTruncation" />
                            <Label IsVisible="{Binding HasNextLesson}"
                                   Text="{Binding NextLessonTitle, StringFormat='Next: {0} →'}"
                                   TextColor="{StaticResource Primary}"
                                   FontSize="13" Margin="0,2,0,0">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding NextLessonCommand}" />
                                </Label.GestureRecognizers>
                            </Label>
                        </VerticalStackLayout>
                        <Button Grid.Column="1"
                                Command="{Binding MarkCompleteCommand}"
                                IsEnabled="{Binding IsCompleted, Converter={StaticResource InvertedBoolConverter}}"
                                BackgroundColor="{StaticResource Primary}"
                                TextColor="White"
                                CornerRadius="16"
                                FontAttributes="Bold"
                                FontSize="14"
                                Padding="20,0"
                                HeightRequest="44">
                            <Button.Shadow>
                                <Shadow Brush="{StaticResource Primary}" Offset="0,4" Radius="12" Opacity="0.4" />
                            </Button.Shadow>
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding IsCompleted}" Value="True">
                                    <Setter Property="Text" Value="Done" />
                                </DataTrigger>
                                <DataTrigger TargetType="Button" Binding="{Binding IsCompleted}" Value="False">
                                    <Setter Property="Text" Value="Mark Complete" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                    </Grid>
                </Border>

                <!-- Bottom spacer -->
                <ContentView HeightRequest="60" />

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
